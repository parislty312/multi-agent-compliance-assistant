# Benchmark Cases

The Week 1 benchmark contains synthetic AI product launch scenarios. Each JSON
file has:

- `case`: input accepted by the case intake schema
- `expected`: the minimum expected decision outcome and risk categories
- `rationale`: a short explanation for human reviewers

These labels are product requirements for testing the future orchestration
workflow. They are not legal conclusions.

